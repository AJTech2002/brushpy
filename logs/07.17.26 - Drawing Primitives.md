
- [ ] Full screen compute abstraction test
- [ ] Compositor compute abstraction with regional rendering (render compute uv to a certain region)
	- [ ] Support blendFn 

I feel like both these abstractions can be combined into one, what do I actually need?

A texture that I can write to a certain region with a given compute shader, does the texture need abstraction or should I just use a MTL Texture, for now I don't think I need the texture abstractions because all the things I can do to the texture will run from compute.

- Compute
	- CompositeCompute
		- `outputTexture` : 0
		- ``

### Goal

Try to render out a pattern to a certain region onto the output texture with Compute, then get all the surrounding architectural work sorted.
