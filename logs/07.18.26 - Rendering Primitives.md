I don't actually see why primitives should get stored in the layer, a layer is simply a texture - when you do:

- `add (primtive)` - this should run the compute once and blit ontop of the texture, you cannot go back and change how the primitive was within the layer - this makes sense! it's rasterized on.
- you build the art work as you go so you always have the final result

```python

canvas = Canvas()

layer = Layer("test")

canvas.addLayer(layer) 

## This is not needed

layer.shift (area, transform)

```

The whole thing is dynamic composition, the canvas itself blits layers in.

- Do you even need a layer, why not blit primitives directly onto the canvas?
	- Layers give sorting order
	- Group primitives 
	- Rsterize primitives
	- Primitive compute shaders should be given access to what colors are behind them
- Should the canvas  blit layers as they are added, or is there any use in re-doing them? I think you should do it because layers have ordering and you should be able to add things to a layer later on and allolw for a re-draw because you may not have all the answers at the start, primitives is fine because they are like programs that runb on the layerl
- Is Tiling even needed if I am not going to dynamically update the primitives? 
	- At this stage it is not needed
	- For brush strokes 

==

### Issues

- Why does each `Primitive` need its own Compute instance, when all the inputs are the same and all it will do is output a color - however there will be custom bindings for different primitives, which becomes much easier if it handles it, abstract when needed - revisit when creating second primitive


How do I want to create primitives? As a function?

```C++

Square:

	Square (width, height, color);

# Uniforms to Compute Shader
	- width
	- height
	- color

# Dynamic uniform dependeing on where it is rasterized
	- transform

# Shader
	- fn() {
	  // POS is crucial, then primitives don't need a compute shader they are just a function 
			return """
			if (POS.x < square.x) {
				COL = float4(1.0, 0.0, 0.0, 0.0);
			}
			""";
	  }

```


==

